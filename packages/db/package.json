{
  "name": "@snackspot/db",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "generate": "prisma generate --schema=prisma/schema.prisma",
    "migrate": "node scripts/migrate.mjs",
    "seed": "node scripts/seed.mjs",
    "studio": "pnpm exec prisma studio --schema=prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0",
    "prisma": "^5.22.0"
  }
}
