# Phase 1: Foundation + Export Pipeline - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-04-06
**Phase:** 01-foundation-export-pipeline
**Areas discussed:** Export UI & feedback, ZIP archive structure, Table & data scope, Photo scope

---

## Export UI & feedback

### Export button placement

| Option | Description | Selected |
|--------|-------------|----------|
| Dedicated page | New "Export / Import" page in sidebar nav — button + future import UI live here | ✓ |
| On stats/overview page | Add export button to existing stats dashboard | |
| Top-level action in nav | Export button in top navigation bar, always visible | |

**User's choice:** Dedicated page
**Notes:** Clean separation from other admin tools. Import UI will live here too in Phase 2.

### Export feedback

| Option | Description | Selected |
|--------|-------------|----------|
| Disabled button + spinner | Button shows spinner and disables during generation. Browser handles download. | ✓ |
| Progress percentage | Show approximate progress via SSE or polling | |
| Just disable the button | Minimal — no spinner, just disabled state | |

**User's choice:** Disabled button + spinner
**Notes:** No streaming progress needed — keeps implementation simple.

---

## ZIP archive structure

| Option | Description | Selected |
|--------|-------------|----------|
| Nested folders | data/ for JSON, photos/ for binaries, manifest.json at root | ✓ |
| Flat structure | All files at root level | |
| Nested with variants | Nested with per-photo subdirectories for original + variants | |

**User's choice:** Nested folders
**Notes:** Clean and predictable. One JSON per table in data/, photos flat in photos/.

---

## Table & data scope

### Table selection

| Option | Description | Selected |
|--------|-------------|----------|
| All non-token tables | Export all 18 tables, only token tables excluded | ✓ |
| Core + relations only | Skip operational tables (Report, ModerationAction, Notification, etc.) | |
| Custom pick | User specifies exactly which tables | |

**User's choice:** All non-token tables
**Notes:** Complete backup — all 18 tables included.

### Decimal serialization

| Option | Description | Selected |
|--------|-------------|----------|
| As numbers | Prisma Decimal → JSON number (4.5) | ✓ |
| As strings | Prisma Decimal → JSON string ("4.5") | |

**User's choice:** As numbers
**Notes:** Requires explicit conversion from Prisma Decimal objects to avoid empty object serialization.

---

## Photo scope

| Option | Description | Selected |
|--------|-------------|----------|
| Originals only | Export original uploaded photos, variants regenerated on import | ✓ |
| Originals + all variants | Export original plus 3 WebP variants per photo | |
| Variants only | Export only processed WebP variants, no originals | |

**User's choice:** Originals only
**Notes:** Smaller ZIP, faster export. Variants can be regenerated by the worker after import.

---

## Claude's Discretion

- Manifest.json schema details (beyond required schema version)
- Streaming implementation specifics (archiver config, compression level)
- Admin MinIO client module structure
- Error handling for missing photos during export
- Export API route naming

## Deferred Ideas

None — discussion stayed within phase scope
