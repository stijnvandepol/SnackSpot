# Phase 1: Foundation + Export Pipeline - Context

**Gathered:** 2026-04-06
**Status:** Ready for planning

<domain>
## Phase Boundary

Admins can download a complete ZIP archive of all database records and photos from the dashboard. This phase delivers shared types, an admin MinIO client, and the full streaming export pipeline with manifest. Import functionality is a separate phase.

</domain>

<decisions>
## Implementation Decisions

### Export UI & feedback
- **D-01:** Dedicated "Export / Import" page in the admin sidebar navigation — export button lives here, import UI will be added in Phase 2
- **D-02:** During export generation, the button shows a spinner and disables. No progress streaming needed — browser handles the download when the response completes

### ZIP archive structure
- **D-03:** Nested folder layout inside the ZIP: `data/` for JSON files (one per table), `photos/` for photo binaries, `manifest.json` at root
- **D-04:** Each table gets its own JSON file in `data/` (e.g., `data/users.json`, `data/places.json`, `data/reviews.json`)
- **D-05:** Photos stored flat in `photos/` by their storage key / photo ID

### Table & data scope
- **D-06:** Export all 18 non-token tables: User, Place, Review, Photo, ReviewPhoto, Comment, Badge, UserBadge, ReviewLike, Favorite, Report, ModerationAction, Notification, NotificationPreferences, ReviewMention, ReviewTag, BlockedWord, FlaggedComment
- **D-07:** Exclude token tables: RefreshToken, PasswordResetToken, EmailVerificationToken (security + operationally useless cross-instance)
- **D-08:** Decimal ratings (Prisma `Decimal` type) serialized as JSON numbers (e.g., `4.5`), not strings or objects — requires explicit conversion from Prisma Decimal objects
- **D-09:** PostGIS geography data serialized as GeoJSON `{lat, lng}` objects (per EXP-03)
- **D-10:** JSON columns (Photo.variants, Photo.metadata) exported as-is — they're already JSON-serializable

### Photo scope
- **D-11:** Export original uploaded photos only (stored as `{photoId}` in MinIO). Processed variants (thumb/medium/large WebP) are excluded — they can be regenerated by the worker after import
- **D-12:** Photos stored in ZIP as `photos/{storageKey}` — flat, no subdirectories per photo

### Claude's Discretion
- Exact manifest.json schema (must include schema version per INF-03, other metadata fields are flexible)
- Streaming implementation details (archiver configuration, compression level)
- Admin MinIO client module structure (following existing `apps/web/lib/minio.ts` pattern)
- Error handling for missing/inaccessible photos during export (skip with warning vs fail)
- Export API route structure (`/api/export` or similar)

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Database schema
- `packages/db/prisma/schema.prisma` — Full data model with all 18+ tables, FK relationships, compound keys, PostGIS geography type, Decimal ratings, JSON columns

### Existing admin infrastructure
- `apps/admin/lib/auth.ts` — `requireAdmin()` middleware pattern, JWT auth, cookie handling — all new endpoints must use this
- `apps/admin/lib/env.ts` — Zod-validated env vars for admin app — MinIO vars will need to be added here
- `apps/admin/lib/db.ts` — Prisma client singleton for admin app

### MinIO integration (reference implementation)
- `apps/web/lib/minio.ts` — Existing MinIO client setup, bucket management, path conventions (`{photoId}` for originals, `variants/{photoId}/{size}.webp` for variants)

### Requirements
- `.planning/REQUIREMENTS.md` — EXP-01 through EXP-08, INF-01 through INF-03 — acceptance criteria for this phase

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets
- `apps/admin/lib/auth.ts` — `requireAdmin()` returns `AdminTokenPayload | Response`, same pattern used by all 16 existing admin API routes
- `apps/admin/lib/db.ts` — Prisma client singleton, reuse for all database queries
- `apps/admin/lib/env.ts` — Zod env schema, extend with MINIO_* variables for admin MinIO client
- `apps/web/lib/minio.ts` — Reference for MinIO client setup (`new Minio.Client(...)`, `ensureBucket()`, bucket config)

### Established Patterns
- Admin API routes: `apps/admin/app/api/{resource}/route.ts` with GET/POST/PATCH/DELETE handlers
- Auth guard: `const auth = requireAdmin(req); if (auth instanceof Response) return auth;` at top of every handler
- Response helpers: Direct `Response.json()` calls (admin app doesn't use the web app's `ok()`/`err()` helpers)
- Env validation: Zod schema in `lib/env.ts` with `.safeParse(process.env)` at module load

### Integration Points
- New sidebar nav item in admin dashboard layout for "Export / Import" page
- New API route: `apps/admin/app/api/export/route.ts` (GET handler, streams ZIP response)
- New MinIO client: `apps/admin/lib/minio.ts` (mirrors web app pattern, reads from extended env)
- New shared types: export manifest type, table record types for serialization

</code_context>

<specifics>
## Specific Ideas

No specific requirements — open to standard approaches

</specifics>

<deferred>
## Deferred Ideas

None — discussion stayed within phase scope

</deferred>

---

*Phase: 01-foundation-export-pipeline*
*Context gathered: 2026-04-06*
