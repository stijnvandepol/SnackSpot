name: Deploy SnackSpot (Self-Hosted)

on:
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - 'packages/**'
      - 'infra/**'
      - '.github/workflows/deploy-selfhosted.yml'
      - 'package.json'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.base.json'
  workflow_dispatch:

concurrency:
  group: snackspot-production
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted
    timeout-minutes: 45

    env:
      COMPOSE_FILE: infra/docker/docker-compose.yml

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Validate Docker availability
        shell: bash
        run: |
          docker version
          docker compose version

      - name: Write environment file
        shell: bash
        run: |
          if [ -z "${{ secrets.SNACKSPOT_ENV_FILE }}" ]; then
            echo "GitHub secret SNACKSPOT_ENV_FILE is missing."
            exit 1
          fi
          printf '%s' "${{ secrets.SNACKSPOT_ENV_FILE }}" > .env

      - name: Build and start stack
        shell: bash
        run: |
          docker compose --env-file .env -f "$COMPOSE_FILE" pull
          docker compose --env-file .env -f "$COMPOSE_FILE" build --pull
          docker compose --env-file .env -f "$COMPOSE_FILE" up -d --remove-orphans

      - name: Run database migrations
        shell: bash
        run: |
          docker compose --env-file .env -f "$COMPOSE_FILE" run --rm api pnpm --filter @snackspot/db migrate

      - name: Verify API health
        shell: bash
        run: |
          max_attempts=20
          delay_seconds=3
          for i in $(seq 1 "$max_attempts"); do
            if curl -fsS "http://localhost:4000/health" >/dev/null; then
              echo "API is healthy."
              exit 0
            fi
            sleep "$delay_seconds"
          done
          echo "API health check failed after multiple attempts."
          exit 1

      - name: Cleanup dangling images
        shell: bash
        run: |
          docker image prune -f
